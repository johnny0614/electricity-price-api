# Authentication Configuration
# JWT secret key for signing tokens (use a strong, random secret in production)
JWT_SECRET=your-super-secret-jwt-key-here

# API User Credentials
# Legacy format (single user)
API_USERNAME=admin
API_PASSWORD=your-secure-password

# New format (multiple users) - optional, overrides legacy format if set
# API_USERS=admin:password123,user2:password456

# Data Configuration
# Path to the CSV data file
CSV_DATA_PATH=data/coding_challenge_prices.csv

# Server Configuration (optional)
PORT=8080
NODE_ENV=development